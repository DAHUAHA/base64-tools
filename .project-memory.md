# Project Memory — base64.tools / zisaac.com

## 关键身份
- **站点品牌名**：base64.tools
- **实际域名**：zisaac.com（base64.tools 已被人注册，无法购买）
- **部署地址**：https://www.zisaac.com
- **Vercel 项目**：dahuahas-projects/base64-tools

## 基础架构
- 静态 HTML 文件部署，无框架，纯前端工具
- 共享样式：`/styles.css`
- 工具页面：index(Base64), json, hash, timestamp, hex, url, privacy, terms
- 静态文件：`sitemap.xml`, `robots.txt`, `analytics.js`

## 关键配置
- **AdSense**：`ca-pub-6093090676087982`
- **Cloudflare Email Routing**：`contact@zisaac.com` → `285233224@qq.com`（已配置 MX/TXT DNS）
- **Vercel Analytics**：已开启

## 部署方式
- `cd "New project 2/base64tool" && npx vercel --prod --yes`（CLI 部署，不用 Git push 自动部署）
- Git push 不会自动触发可靠部署

## 待完成
- AdSense 收款信息填写（后台完成）
- 等 Google 人工审核（1-2 天）
- SEO：OpenGraph + Twitter Card meta

## 历史问题
- `public/` 目录会导致 Vercel 输出目录切换，造成 404，勿单独创建
- index.html 曾被截断，JS 代码前缺少 `<script>` 标签
- Cloudflare DNS 满时需清理旧记录才能加新的