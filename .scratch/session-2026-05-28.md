# Session Summary — 2026-05-28

## 解决的问题

### 1. Vercel 部署 404（关键教训）
- **根因**：`public/ads.txt` 创建了 `public/` 目录，导致 Vercel "Other" 框架预设将输出目录从 `.` 切换到 `public/`，所有根目录的 HTML 文件无法访问
- **修复**：删除 `public/` 目录，ads.txt 只放在根目录
- **部署方式**：Git push 自动部署不可靠，改用 `vercel --prod --yes` CLI 部署

### 2. 广告拦截收入挽回
- AdSense → 隐私权和消息 → 广告拦截收入挽回
- 8 个页面均已添加 Funding Choices 脚本
- CSP 已更新允许 `fundingchoicesmessages.google.com`

### 3. 安全修复
- GET /api/logs 无鉴权，公开暴露用户转换记录 → 去掉 GET，只保留 POST

### 4. Vercel Web Analytics
- 已添加 analytics.js + /_vercel/insights/script.js 到 8 个页面
- 需在 Vercel Dashboard 手动开启 Web Analytics

### 5. GitHub Token 泄露
- npm init 时 package.json 自动包含了带 token 的 git remote URL
- 旧 token 已吊销，新 token 已配置

## 当前项目状态

- 域名：www.zisaac.com / base64.tools
- 8 个页面：index, json, hash, timestamp, hex, url, privacy, terms
- 共用文件：styles.css, theme.js, analytics.js
- 部署：Vercel CLI (`vercel --prod --yes`)
- 待处理：AdSense ads.txt 等待 Google 自动复检

## 今日补充进度（Mavis）

### 1. index.html 结构修复
- 文件被截断，JS 代码前缺少 `<script>` 标签，浏览器直接显示源码
- 修复：添加 `<script>` 标签，补全 JS 函数（encode/decode/autoEncode/autoDecode/switchTab/copyOutput/handleFile/downloadOutput/showToast/history 等）
- 同步修复 privacy.html 和 terms.html 的结构问题

### 2. Cloudflare Email Routing 配置
- 域名：zisaac.com（托管在 Cloudflare）
- 目的：让 `contact@zisaac.com` 真实可收邮件，转发至 QQ 邮箱
- 通过 Cloudflare API Token（`cfat_UCY7PqYI...`）添加 DNS 记录：
  - MX × 3 条（route1/2/3.mx.cloudflare.net）
  - TXT DKIM 记录（cf2024-1._domainkey）
  - TXT SPF 记录
- 验证方式：发送确认邮件到 `285233224@qq.com`

### 3. AdSense 申请完成
- 隐私政策邮箱 `contact@zisaac.com` 现已真实可用（Email Routing）
- AdSense 脚本（`ca-pub-6093090676087982`）已添加到全部 6 个工具页面
- 网站关联验证：通过（AdSense 检测到脚本代码）
- GDPR CMP：采用 Google 自带 CMP，3 选项（同意/不同意/管理），已集成到所有页面
- 收款信息：待填写（需在 AdSense 后台完成）
- 等待 Google 人工审核（通常 1-2 天）

## 推广文案

V2EX 发帖模板已写好，见 2026-05-28 聊天记录。
